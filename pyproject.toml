[project]
name = "novel-auto-agent"
version = "1.0.0"
description = "AI视频分镜Prompt生成系统 + Jellyfish前后端"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests", "backend/tests"]
pythonpath = ["."]
